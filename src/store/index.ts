import { createStore } from 'vuex'
import {UserAuth} from "@/store/UserAuth";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    UserAuth
  }
})
